extends: relaxed

rules:
  line-length: disable
  new-line-at-end-of-file: disable

ignore: |
  .yamllint.yml
  manifests/deploy-secrets/*.secret.yaml
  charts/*/charts/
  charts/*/templates/
  kustomize/bases/*/charts/
  kustomize/*/*/*.secret.yaml
