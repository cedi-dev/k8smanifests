csi-hcloud:
  storageClasses:
    - name: hcloud-volumes
      defaultStorageClass: true
      reclaimPolicy: Retain

  prometheus:
    serviceMonitor:
      enabled: true
      namespace: "kube-system"
