csi-hcloud:
  storageClasses:
    - name: hcloud-volumes
      defaultStorageClass: true
      reclaimPolicy: Delete

  prometheus:
    serviceMonitor:
      enabled: true
      namespace: "kube-system"
