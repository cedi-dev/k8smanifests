external-dns:
  fullnameOverride: external-dns
  provider: cloudflare
  cloudflare:
    proxied: false
  txtOwnerId: "k8s"
  policy: sync
  logFormat: json
  sources:
    - ingress
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
