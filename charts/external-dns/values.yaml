external-dns:
  fullnameOverride: external-dns
  provider: hetzner
  hetzner:
    secretName: cloud-provider-credentials
    secretKey: HZ_DNS_TOKEN
  txtOwnerId: "k8s"
  policy: sync
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
