loki-stack:
  loki:
    persistence:
      enabled: false
      accessModes:
        - ReadWriteOnce
      size: 30Gi

    serviceMonitor:
      enabled: true

  promtail:
    serviceMonitor:
      enabled: true