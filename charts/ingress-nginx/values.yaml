fullnameOverride: ingress-nginx

controller:
  replicaCount: 3

  extraArgs:
    enable-ssl-passthrough: ''

  service:
    externalTrafficPolicy: "Local"

  metrics:
    serviceMonitor:
      enabled: true
