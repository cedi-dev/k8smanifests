ingress-nginx:
  fullnameOverride: ingress-nginx
  controller:
    image:
      image: docker/ingress-nginx/controller
    extraArgs:
      enable-ssl-passthrough: ''
    replicaCount: 3
    service:
      externalTrafficPolicy: "Local"
    adminssionWebhooks:
      patch:
        image:
          image: docker/jettech/kube-webhook-certgen
    serviceMonitor:
      enabled: true
  defaultBackend:
    enabled: true
    image:
      image: docker/defaultbackend-amd64
