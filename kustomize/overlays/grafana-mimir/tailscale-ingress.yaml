---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mimir
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mimir-nginx
                port:
                  number: 80
