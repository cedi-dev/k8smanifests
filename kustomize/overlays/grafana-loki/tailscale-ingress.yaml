---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loki
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loki-gateway
                port:
                  number: 80
