apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd

resources:
  - ../../bases/argocd/

generators:
  - ./secret-generator.yaml

patches:
  - target:
      kind: Deployment
      namespace: argocd
      name: argocd-server
    path: tailscale-sidecar.patch.yaml