- op: add
  path: "/spec/template/spec/containers/-"
  value:
    name: ts-sidecar
    imagePullPolicy: Always
    image: ghcr.io/tailscale/tailscale:v1.32.1
    env:
      - name: TS_KUBE_SECRET
        value: arocd-server-tailscale-auth
      - name: TS_USERSPACE
        value: "true"
      - name: TS_AUTH_KEY
        valueFrom:
          secretKeyRef:
            name: arocd-server-tailscale-auth
            key: AUTH_KEY
            optional: true
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      capabilities:
        add:
          - NET_ADMIN
          - SYS_MODULE
