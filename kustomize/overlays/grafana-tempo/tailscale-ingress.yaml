---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tempo
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tempo-distributor
                port:
                  number: 4318
