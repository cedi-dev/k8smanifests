metaMonitoring:
  serviceMonitor:
    enabled: true

minio:
  enabled: false

query_scheduler:
  enabled: false

nginx:
  replicas: 1
  resources:
    limits:
      cpu: 250m
      memory: 2048Mi
    requests:
      cpu: 125m
      memory: 512Mi
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 3

ingester:
  # TODO: when re-enabling zoneAwareReplication, increase this to double the amount of zomes
  replicas: 3
  persistentVolume:
    size: 30Gi
  zoneAwareReplication:
    enabled: false
    # TODO: One-day re-enable this with multiple hcloud zones
    #maxUnavailable: 1
    #topologyKey: 'kubernetes.io/hostname'
    #zones:
    #- name: fsn1-a
    #  nodeSelector:
    #    topology.kubernetes.io/region: fsn1
    #- name: fsn1-b
    #  nodeSelector:
    #    topology.kubernetes.io/region: fsn1
    #- name: fsn1-c
    #  nodeSelector:
    #    topology.kubernetes.io/region: fsn1

store_gateway:
  replicas: 3
  persistentVolume:
    size: 2Gi
  # TODO: One-day re-enable this with multiple hcloud zones
  zoneAwareReplication:
    enabled: false

compactor:
  persistentVolume:
    size: 90Gi

distributor:
  replicas: 2