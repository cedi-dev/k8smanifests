apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: minio
helmChartInflationGenerator:
  - chartName: minio
    chartRepoUrl: https://charts.bitnami.com/bitnami
    chartVersion: 11.10.12
    releaseName: minio
    releaseNamespace: minio
    values: helm-values.yaml
