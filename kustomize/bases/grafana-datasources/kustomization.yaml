apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability

configMapGenerator:
  - name: grafana
    namespace: observability
    files:
      - grafana.yaml

  - name: prom-operator
    namespace: observability
    files:
      - prom-operator.yaml
