ingress:
  enabled: true
  tls: true
  hostname: auth.cedi.dev
  certManager: true
  annotations:
    "cert-manager.io/cluster-issuer": "letsencrypt-prod"
extraArgs:
  - --provider=github
  - --github-org=cedi-dev
  - --cookie-domain=cedi.dev
  - --whitelist-domain=.cedi.dev
redis:
  auth:
    enabled: false
configuration:
  clientID: MTIzNA==
  clientSecret: MTIzNA==
  cookieSecret: MTIzNA==