apiVersion: monitoring.grafana.com/v1alpha1
kind: PodLogs
metadata:
  labels:
    instance: primary
  name: kubernetes-pods
  namespace: observability
spec:
  pipelineStages:
    - docker: {}
  namespaceSelector:
    matchNames:
      - "*"
  selector:
    matchLabels: {}